function cadastrarProtocolo() {
    event.preventDefault()
    let mock = true;
    if( mock == true){
        alert("Protocolo cadastrado com sucesso")
        window.location.replace("retornoProtocolo.html")
    }
}